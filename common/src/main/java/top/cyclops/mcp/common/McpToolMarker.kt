package top.cyclops.mcp.common

/**
 * Marker interface for MCP tool classes.
 * Classes implementing this interface will be registered as MCP tools
 * and available via dynamic proxy generation.
 */
interface McpToolMarker